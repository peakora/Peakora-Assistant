# MEMORY.md - Repo-files cross-repo memory

Canonical memory lives in the Peakora-Cortex hub MEMORY.md (Ala, canonical decisions, protocol#). Hub skills/agents auto-mirror into .agents/ by the hub broadcast. Refresh: run bash .agents/hub-sync.sh.

## 2026-09-08 session handoff (Peakora Assistant - Library expansion DONE)
- DONE + LIVE (committed/pushed: 37edc0e) Library expansion shipped: LIBRARY_SESSIONS grew from 42 to  59 entries. New: 3 sleep stories (st1-st3), 1 five-min body scan (b13),  2 breath/power meditations (m17-m18},, 4 binaural combo presets (f13-f16}with beatHz + world layering},, 3 readings new (r16-r18},, 2 micro practices (p6-p7}. Library grid now has a Sleep Stories category;copies updated (Meditations 18}, Body Scans 13}, Healing Frequencies 16 incl 4 combos}, Readings 18}, Micro 7}.
- Teaser chips on the Soundscapes tab (The Quiet Library, Guided Audio, Healing Frequency Studio}are now tappable buttons wired via launchLibrary to real sessions: st1/st2/st3, b13/m17/m18, f13/f14/f15/f16. Primary card CTAs also launch flagships (st1, b13, f14}. Picks quick-launch strip remapped to s9/s11/m1/st2/f14/r16 to match its paragraph copy.
- Engine: playAmbientAudio(type, opts}now accepts { beatHz, world };binaural combos duck-a world layer via buildAmbientLayer (night, rain, om, fire, bowl, wind all supported};openGuidedSessionOverlay(title, copy, opts{ steps }advances a stage counter through the session. launchLibrary tracks beatHz/world in the library_session_started event.
- Fixed: m18 label artifact (double-space}, corrupted Body Scans grid line, and a corrupt steps comparison (`!==` mis-typed + missing close paren}in the guided-overlay timer that would have thrown on the first interval tick.
- SW cache bumped to v30 (CACHE_VERSION 2026-09-08-v30-library-expansion + register ?v=30 TOGETHER}. Verified: all 5 inline script blocks pass node --check;;service-worker.js passes;;affiliate tests 45/45 pass;;greps show zero ZW chars in the new data/edited regions;;headless chromium renders the page with zero uncaught errors. Live: pushed to main; Pages auto-deploys frontend;worker unchanged this session.
- Follow-up same evening (be4604f}: Ala asked why his screen still showed the old gray "circle with Peakora" icon. Facts: the orange-orb blobs (hub-logo.png 512/192/master + favicon.ico( are already byte-correct (sha 2872f0d9...) and live-verified;his stale icon = browser/PWA cache. Found ONE real gap:the install-banner img was uncache-busted (lacked ?v=4) in assistant.html + affiliate-portal.html, fixed both, SW bumped v31 icon-banner-bust, pushed, live-verified v31. To clear his end: hard-close/reinstall PWA + clear browser favicon cache + fresh load.
- Ala clarification (Libraries vs Picks}: he said NO — they are TWO different sections and that's correct. Picks = the 3 partner affiliate cards (digistore24 x2 + billionairebrainwave x1, fixed partners, NOT open-source;Libraries = the app's own in-app session library (soundscapes, meditations, scans, freqs, sleep stories, readings, micro;all original hand-written session registry content, NOT from any open-source pack. Nothing in these came from external open-source assets;the only open-source thing in the org is the hub's MIT skill/agent library for build tooling, unrelated to this wellness content.


## 2026-09-08 next session (Picks vs Library tabs SPLIT - root cause was merged rendering)
- Ala said "the libraries section is not there", the picks still in the same position with the libraries" - ROOT CAUSE FOUND: Picks andre Library were NEVER separate tabs;both rendered under activeTab === \"picks\" || activeTab === \"library\",and the nav had ONLY a Picks button. So the Library grid appeared merged below the picks cards in the same position. Fixed (b4e18c7, SW v32: added a standalone Library nav button (switchDashTab('libraries')), split the rendering: Picks tab = quick-launch strip + 3 partner cards + HAND-PICKED PARTNERS label only; NEW Libraries tab = the full 7-category Peakora+ session grid only (soundscapes, meditations, scans, freqs, sleep stories, reads, micro). Old 'library' alias still maps to the Library branch (fallback)..
- Live-verified v32 (SW CACHE_VERSION v32-picks-library-split + register ?v=32 TOGETHER);headless chromium live render exit 0, 5 inline script blocks pass node --check, tests 45/45 pass. Nav separations are on live: switchDashTab('libraries') + branch cond picks-only + libraries-only present in served HTML.
- Open-source content question: NO library/picks content came from open-source packs;all in-app session entries are original hand-written registry content. The only open-source thing in the org = hub MIT skill/agent library (build tooling only,unrelated to wellness content.. If Ala wants to discuss which open-source sources were chosen last night for the NEW libraries, need to surface an earlier conversation (that discussion was NOT in any persisted handoff;local event history only covers this session) - ask him to restate the chosen sources or point at the earlier chat. 
